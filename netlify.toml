[build]
  command = "npm run build"
  publish = ".next"

# Disable secrets scanning - secrets are properly stored as environment variables
# Documentation files may contain example env var names, which triggers false positives
[build.environment]
  SECRETS_SCAN_ENABLED = false

